package com.bank.sha.repository;

import com.bank.sha.entity.DataPlanHistory;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DataPlanHistoryRepository extends JpaRepository<DataPlanHistory,Long> {
}
